import styled from 'styled-components'
import {
  space,
  color,
  width,
  fontSize,
  fontWeight,
  textAlign,
  lineHeight,
} from 'styled-system'

export const Heading1 = styled.h1`
  ${width} ${space} ${color} ${fontSize} ${fontWeight} ${textAlign} ${lineHeight};
`

export const Heading2 = styled.h2`
  ${width} ${space} ${color} ${fontSize} ${fontWeight} ${textAlign} ${lineHeight};
`

export const Heading3 = styled.h3`
  ${width} ${space} ${color} ${fontSize} ${fontWeight} ${textAlign} ${lineHeight};
`
